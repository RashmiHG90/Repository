client side synchronous and asynchronous.
To make code asynchronous at client side javascript we need to use 3 functions

setTimeOut(callback, time): it execute only once after specific time.

setInterval( callback, time): it execute again and againafter specific time.

clearInterval(): to stop setInterval